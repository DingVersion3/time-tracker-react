# ⏱️ Time Tracker — React + FastAPI

A mobile-friendly time tracking app for childcare businesses with variable hourly rates.


To-Dos:
database: postgresql
auth: jwt tokens
    -user signs up with email/password
    - backend issues token, frontend stores it and sends it with every request
    -passlib for password hashing, python-jose for jwt tokens
multi-tenancy
payments
onboarding